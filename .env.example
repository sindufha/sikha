# Database — ganti dengan connection string PostgreSQL/Neon kamu
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Auth
JWT_SECRET="ganti-dengan-random-secret-panjang"
JWT_EXPIRES_IN="7d"
